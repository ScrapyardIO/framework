# Directory Update Log

## 2026-08-07

* **Update**: [sketches](components/sketches.md) — Nodes/Flows are a general orchestration API (not sketch-only); recommended for sketch workflows + AI assistant step graphs; points at website Guide **Nodes & Flows**.
* **Creation**: `workshop make:node` / `make:node --async` → `App\Workflows` (`NodeMakeCommand` + stubs). Product wording: **decision-based Logic Orchestration** (Guide drops PocketFlow name).

* **Creation**: Draft [sketches](components/sketches.md) — `fabricate/sketches` for 0.7: `php runner` / `Machine::handleSketch` + SketchKernel; Pipeline middleware; PocketFlow-stolen Flow/AsyncNode; discovery `app/Runner/Sketches`; `make:sketch` / `make:middleware`; Concurrency `fiber` + suggested `pokio`. Pest `tests/Sketches/*`. Umbrella `replace` `fabricate/sketches`.
* **Update**: [concurrency](components/concurrency.md) — `FiberDriver` + `PokioDriver` (suggest `nunomaduro/pokio`); Fiber helpers; config documents fiber/pokio.
* **Update**: Workshop migrate/seed/models — Core `MigrationServiceProvider`; commands `migrate`, `migrate:rollback`, `migrate:status`, `migrate:install`, `make:migration`, `db:seed`, `make:seeder`, `make:model`, `make:graph-model`; `Schema` Magic Alias; no separate `graph:migrate`. Composer suggests for PDO drivers + laudis. Guide **Polisher ORM** nav group. Pest `MigrateAndSeedTest`. Updated [database](components/database.md), [polisher](components/polisher.md), [graph](components/graph.md).
* **Creation**: Draft [graph](components/graph.md) — optional `fabricate/graph` Neo4j companion (adapted from private eloquent4j onto Database + Polisher). `Neo4jConnector` / `Neo4jConnection` / `Graph\Polisher\Model` / `cypher*` helpers / opt-in `GraphServiceProvider` (not in DefaultProviders). Umbrella `replace` + suggest `laudis/neo4j-php-client`. Pest smoke without live Neo4j. Guide/SDK `graph`. No Auth/Session/AWS.
* **Creation**: Draft [http](components/http.md) — outbound-only `Fabricate\Http\Client` over Guzzle with fluent requests/responses, pools, batches, events, exceptions, fakes, deferred Core provider, `Http` Magic Alias, Machine aliases, split-package metadata, and focused Pest coverage. Explicitly excludes inbound kernel/routing, Auth, Session, and AWS.
* **Update**: Queue public drivers now include `database` (DatabaseConnector). Removed SQS/Beanstalkd connector/job classes (no AWS). Updated config/queue.php + Pest + OKF.
* **Creation**: Database + Polisher Phase 1/2 — Illuminate Database v12 adapted to `Fabricate\Database` with Polisher naming, SQLite/MySQL/PostgreSQL only; Pagination/contracts + support dependencies restored; Core `DatabaseServiceProvider` / `DB` MagicAlias / Machine aliases; `config/database.php`; Queue model serialization points to Polisher; component packaging; SQLite query-builder + Polisher CRUD Pest coverage; Guide/SDK synced. Draft [database](components/database.md), [polisher](components/polisher.md).

* **Creation**: Stable convention [AWS is not first-class](conventions/aws-not-first-class.md) — Angel verified. Local desktop + edge/consumer only; no public SQS/DynamoDB/S3. Aligns Cache (file+redis) and Queue Phase 1. Guide queues/filesystem wording tightened; docs:sync.
* **Update**: Workshop `about` — lean Environment/Cache/Drivers only (no ICs/sensors/displays). Extensible via `AboutCommand::add()` from package providers. Added `NutsAndBolts\Composer`, `machine.debug`. Pest: `tests/Console/AboutCommandTest.php`. Guide/SDK workshop + packages. Updated [console](components/console.md).
* **Creation**: Bus + Queue Phase 1 Core glue — `fabricate/bus` + `fabricate/queue` packaging (^0.7); Core `BusServiceProvider` / `QueueServiceProvider` (`$this->container`); MagicAliases `Bus` / `Queue`; AliasLoader + DefaultProviders; Machine aliases `bus` / `queue` / `queue.connection`; `config/queue.php` (sync/redis/deferred/failover/null; failed null+file); Workshop `queue:work` + `queue:restart`; public connectors null/sync/deferred/background/failover/redis; `Queue\Concerns\ResolvesQueueRoutes` + Nab `ReadsClassAttributes`; umbrella `replace`; Pest `tests/Bus` + `tests/Queue`. Draft [bus](components/bus.md), [queue](components/queue.md). Guide/SDK synced. Out of scope: Database/SQS/Beanstalkd/Dynamo registration, batches, Horizon.
* **Update**: Completed Illuminate/0.6 Events surface for 0.7 (no Queue/Bus/DB tx/Broadcast/Eloquent) — `Dispatcher::defer` + `getRawListeners` + public `hasWildcardListeners`; `NullDispatcher`; `event()` helper; Core `Dispatchable` (no `broadcast`); `Testing\Fakes\EventFake` + `Fake` marker; `Event` MagicAlias fake helpers; Workshop `event:cache` / `event:clear` / `event:list` + `make:listener` (plain/typed stubs only). Pest: `tests/Events/*`, `tests/Console/EventCommandsTest.php`. Guide/SDK events + helpers updated. Updated [events](components/events.md).
* **Update**: Workshop `optimize` / `optimize:clear` (Illuminate Foundation pattern) — runs restored `config:*` + `event:*` (+ `cache:clear` on clear) and merges `ServiceProvider::$optimizeCommands` / `$optimizeClearCommands`. No route/view/compiled yet. Pest: `tests/Console/OptimizeCommandsTest.php`. Guide/SDK workshop + core updated. Updated [console](components/console.md).
* **Creation**: Ported Illuminate Translation + Validation — `fabricate/translation`, `fabricate/validation`; Core `TranslationServiceProvider` / `ValidationServiceProvider`; MagicAliases `Lang` / `Validator`; contracts; NutsAndBolts support (MessageBag, Fluent, NamespacedItemResolver, …); `Machine::langPath()` / locale config. DB presence + NotPwned deferred. Pest + Guide/SDK synced. Draft [translation](components/translation.md), [validation](components/validation.md).
* **Creation**: Ported `fabricate/pipeline` from 0.6 (+ Illuminate parity: `withinTransaction`, Hub). Contracts, Pipeline, Hub; Core `PipelineServiceProvider` + `Pipeline` MagicAlias (`$cached = false`); Machine aliases; `replace` `fabricate/pipeline`. Process packaging completed (`LICENSE.md`, `.gitattributes`). Pest: `tests/Pipeline/PipelineTest.php`. Guide/SDK pipeline synced. Database still required for `withinTransaction()`.
* **Creation**: Ported Illuminate Process + Concurrency — `fabricate/process` (Factory, PendingProcess, Pool, Pipe, fakes, Symfony Process), `fabricate/concurrency` (ConcurrencyManager, sync/process/fork drivers, `invoke-serialized-closure` Workshop command). Core `ProcessServiceProvider` + `ConcurrencyServiceProvider`, MagicAliases `Process` / `Concurrency`, `config/concurrency.php`, `replace` entries, `laravel/serializable-closure` require. `NutsAndBolts\MultipleInstanceManager` + `RebindsCallbacksToSelf`. ProcessDriver uses `WorkshopInstance::formatCommandString` + `SCRAPYARD_IO_INVOKABLE_CLOSURE`. Pest: `tests/Process/ProcessAndConcurrencyTest.php`. Draft [process](components/process.md), [concurrency](components/concurrency.md). Guide/SDK synced.
* **Creation**: Ported `fabricate/hashing` from Illuminate — `HashManager`, bcrypt/argon/argon2id drivers, `Contracts\Hashing\Hasher`, Core `HashServiceProvider` + `Hash` MagicAlias, Machine aliases, `bcrypt()` helper, `config/hashing.php`, `replace` `fabricate/hashing`. Pest: `tests/Hashing/HashTest.php`. Guide/SDK hashing synced. Draft [hashing](components/hashing.md).
* **Creation**: Ported `fabricate/json-schema` from Illuminate — `JsonSchema` static API, `Types/*`, `Serializer`, `Deserializer` + `fromArray`, contract at `Fabricate\Contracts\JsonSchema\JsonSchema`. Pure domain (no Core SP / MagicAlias). Enums: `SupportedUnionType`, `DeserializerLimit`. Pest: `tests/JsonSchema/JsonSchemaTest.php`. Guide + SDK `json-schema` synced. Updated [json-schema](components/json-schema.md), [composer-replace](conventions/composer-replace.md), index.
* **Update**: Completed `fabricate/encryption` component packaging (`composer.json`, `LICENSE.md`, `.gitattributes`), expanded Pest (`GCM`, previous keys, invalid payload), Guide `docs/encryption.md` + SDK refresh, OKF index/concept/component-packages. Runtime was already full (Encrypter + Core SP + Crypt).
* **Update**: Ported 0.6 dev Workshop generators into Core (`ClassMakeCommand`, `ConfigMakeCommand`, `ConsoleMakeCommand` → `make:console`, `EnumMakeCommand`, `EventMakeCommand`, `ExceptionMakeCommand`, `ObserverMakeCommand`, `TraitMakeCommand`, `VendorPublishCommand`) + stubs under `Core/Console/stubs/`. Added `Fabricate\Core\Events\VendorTagPublished`. Registered in `WorkshopServiceProvider` `$devCommands` + `registerVendorPublishCommand()`. Pest: `tests/Console/MakeCommandsTest.php`. Guide/SDK workshop + packages docs synced. Updated [console](components/console.md).
* **Update**: Workshop `config:cache` / `config:clear` ported; `Machine::useStoragePath()` + deferred `make()`/`bound()`/`resolve()` so Cache/Redis providers load on demand. Cache commands already registered (`cache:clear`/`forget`). Guide/SDK config+cache updated.
* **Update**: Full Encryption runtime — `Encrypter` encrypt/decrypt (+ contracts), Core `EncryptionServiceProvider` binds `encrypter` from `machine.key`/`cipher`, `Crypt` MagicAlias in `AliasLoader`, Machine aliases, `encrypt()`/`decrypt()` helpers, `replace` `fabricate/encryption`. Updated [encryption](components/encryption.md).
* **Update**: Cache + Redis Core glue — `Core\Providers\CacheServiceProvider` / `RedisServiceProvider`, MagicAliases, Machine aliases, `replace` `fabricate/cache`+`fabricate/redis`, `psr/simple-cache` require. Public cache drivers **file + redis** only (local/edge; no AWS); `array` for tests/limiter. Draft [cache](components/cache.md), [redis](components/redis.md), [encryption](components/encryption.md). Guide/SDK cache+redis+scheduling synced on docs site. `DayOfWeek` enum replaces Schedule day class constants.
* **Update**: Ported Laravel 13.x schedule Workshop commands into `Fabricate\Console\Scheduling\*` (`schedule:list`, `schedule:run`, `schedule:work`, `schedule:test`, `schedule:finish`, `schedule:clear-cache`, `schedule:pause`, `schedule:resume`, `schedule:interrupt`). Added `CommandBuilder`, `CronExpressionTimezoneConverter`, scheduling events under `Fabricate\Console\Events\`, and `ScheduleSignalKey` enum (`fabricate:schedule:*`). `ConsoleKernel::resolveConsoleSchedule()` wires Schedule + cache store. Fixed `Schedule.php` Illuminate leftovers (Chassis/WorkshopInstance/Program). Registered commands in `WorkshopServiceProvider`. Added `dragonmantank/cron-expression` require. Pest: `tests/Console/ScheduleCommandsTest.php`. Updated [console](components/console.md).
* **Update**: Restored minimal `Fabricate\Encryption\Encrypter` + `Cipher` enum (key generation / supported only; `EncryptionServiceProvider` stays commented in `DefaultProviders`). Core commands `config:show`, `env`, `key:generate` registered in `WorkshopServiceProvider`; `key:generate` writes `.env` via Filesystem (`files`). Expanded framework `config/machine.php` with `env`, `key`, `cipher`, `previous_keys`. App skeleton `.env.example` added. Pest: `tests/Encryption/EncrypterTest.php`, `tests/Console/ConfigCommandsTest.php`. Updated [console](components/console.md).
* **Creation**: Draft playbook [port-from-0.6-or-illuminate](playbooks/port-from-0.6-or-illuminate.md) — every 0.6/Illuminate bring-back must update OKF and check Laravel docs for Guide + API/SDK equivalents, then sync into the docs site. Incomplete without docs+OKF unless Angel waives.
* **Creation**: Pest 4 suite for present 0.7 surface (`tests/` — Collections, Config, Chassis, Events, Filesystem, Log, Nab Str/Conditionable/Macroable, DefaultProviders, ProviderRepository). `composer test` / `vendor/bin/pest` green (27 tests). Added `.github/workflows/tests.yml` (PHP 8.4) and Tests badge on README. Autoload-dev namespace `Tests\\`.

## 2026-08-06

* **Update**: Dropped local `pw` CLI shortcut from [core](components/core.md) / [package](orientation/package.md); public wording is `workshop` only.
* **Update**: Restored `ProviderRepository` + uncommented `Machine::registerConfiguredProviders()` load path; added `Program::addDeferredServices()`. Restored `PackageDiscoverCommand` and registered it in `WorkshopServiceProvider`. App `bootstrap/providers.php` lists `MachineServiceProvider` — loaded automatically via `withProviders()`. Smoke: `workshop list` shows `package:discover`; discover writes `bootstrap/cache/{packages,services}.php`; `files`/`filesystem` bound. Updated [core](components/core.md).
* **Update**: `Machine::getBootstrapProvidersPath()` restored as a concrete method (`bootstrapPath('providers.php')`) and declared on `Program`. `AssemblyLine::withProviders()` now calls a real typed path helper (not an undeclared / MagicAlias-style polymorphic call). Updated [core](components/core.md).
* **Creation**: Draft [log](components/log.md) — ported `fabricate/log` (`LogManager` / `Logger` / `MessageLogged` / config parsers) from 0.6; cleaned `Contracts\Log` to only `ContextLogProcessor`. Core owns `Log` MagicAlias + `LogServiceProvider` (`singletonIf`); Machine early-binds `log` for HandleExceptions; `DefaultProviders` includes Log SP. App + framework `config/logging.php`. **Deferred:** `Context\Repository` / `ContextServiceProvider` (Queue `SerializesModels` + Eloquent). Updated [core](components/core.md), [contracts](components/contracts.md), [composer-replace](conventions/composer-replace.md), index.
* **Creation**: Draft [events](components/events.md) — ported `fabricate/events` Dispatcher + `Contracts\Events\*`; Dispatcher types `ServiceContainer`. Core owns `Event` MagicAlias, `EventServiceProvider`, `DiscoverEvents`, `Terminating`, `AssemblyLine::withEvents()`. Machine binds `events` + cache helpers; bootstrapWith dispatches bootstrapping/bootstrapped; WorkshopInstance/ConsoleKernel/CoreServiceProvider event paths restored (`JobAttempted` / `CallQueuedListener` still deferred). Umbrella requires `symfony/event-dispatcher`. Updated [core](components/core.md), [contracts](components/contracts.md), [console](components/console.md), [composer-replace](conventions/composer-replace.md), index.
* **Update**: Strong-typed `CliDumper` + `ResolvesDumpSource` (typed props/params/returns; safer dump line append; `file_get_contents` failure handled).
* **Update**: Dropped `Core\Http\HtmlDumper` — ScrapyardIO is CLI/Workshop-first; `registerDumper` is CliDumper-only (still skips `server`/`tcp` VAR_DUMPER_FORMAT).
* **Update**: Finished Core dumpers — ported `ResolvesDumpSource`; fixed `CoreServiceProvider::registerDumper` (Symfony `CliDumper` import was shadowing Fabricate’s). Guard empty `view.compiled` in `isCompiledViewFile`.
* **Creation**: Draft [console](components/console.md) — ported full `fabricate/console` from 0.6 (Command/IO/prompts/scheduling/resources). Kept 0.7 `WorkshopInstance` + `CLIKernel`/`CLIMachine`. Core owns `ConsoleSupportServiceProvider` + slim `WorkshopServiceProvider` (empty command map). `Command` types `ServiceContainer`. Nab `Stringable`/`Tappable` brought as Console deps. Illuminate leftovers in Prompts → Fabricate Collection.
* **Update**: Promoted `register` / `resolveProvider` / `hasDebugModeEnabled` from Program onto public [ServiceContainer](../src/Fabricate/Contracts/Chassis/ServiceContainer.php) (they were never on Wireframe). Chassis stubs throw; Machine implements. Unblocks AggregateServiceProvider / CoreServiceProvider typing `$this->container`.
* **Creation**: Draft [filesystem](components/filesystem.md) — ported Manager/Adapters/LockableFile/Visibility from 0.6; `FilesystemServiceProvider` + `Storage` stay in Core; Manager types `ServiceContainer` (not Program); promoted `bound()` onto public ServiceContainer; app + framework `config/filesystems.php`; component requires `^0.7.0`.
* **Update**: `RegisterProviders` uses `Fabricate\Core\DefaultProviders::make()` (not Nab `ServiceProvider::defaultProviders`). Removed illegal Nab→Core `DefaultProviders` import. Documented in [magic-aliases](conventions/magic-aliases.md).
* **Update**: Implemented `resolveEnvironmentUsing` on Chassis; declared on public [ServiceContainer](../src/Fabricate/Contracts/Chassis/ServiceContainer.php) (not Wireframe). Unblocks LoadConfiguration after config load.
* **Creation / Verification**: Angel reports [config](components/config.md) completely ported — `fabricate/config` Repository + `Contracts\Config\Repository`. Marked `stable` / verified. Component composer requires bumped to `^0.7.0`. Refreshed [env-outside-config](traps/env-outside-config.md); contracts + composer-replace + indexes updated.
* **Creation**: Draft [core](components/core.md) — Machine / AssemblyLine `withKernels`+`withExceptions` / ConsoleKernel → WorkshopInstance; CLI milestone `workshop` shows ScrapyardIO Framework 0.7.0 (list/help/completion). Gaps: empty bootstrappers, no ComposerScripts/package:discover, Program TODOs. Updated [contracts](components/contracts.md) (CLIKernel/CLIMachine/Program), [package](orientation/package.md), indexes.
* **Update**: Promoted consumer methods onto [ServiceContainer](../src/Fabricate/Contracts/Chassis/ServiceContainer.php) (`make`/`call`/`instance`/`resolved`/`afterResolving`). Wireframe keeps bind kitchen sink. MagicAlias types `ServiceContainer`; Nab may depend on `fabricate/contracts`. Updated [contracts](components/contracts.md), [chassis](components/chassis.md), [dependency-direction](conventions/dependency-direction.md), [magic-aliases](conventions/magic-aliases.md).
* **Update**: Removed `MagicAliasTracking` trait — `alias` / `extend` / alias cache helpers live on `Chassis` again. Updated [chassis](components/chassis.md).
* **Update**: Filled Chassis Wireframe stubs from 0.6 donor (`instance`, `alias`, `extend`, tags, `*If`, `when`/contextual builder, resolving registration, `flush`, `factory`, `callMethodBinding`, `getInstance`/`setInstance`). Restored `MagicAliasTracking`, `ContextualBindingBuilder`, `RewindableGenerator`. Flipped [chassis](components/chassis.md) back to `draft` (surface grew).
* **Verification**: Angel marked [chassis](components/chassis.md) `status: stable` (`verified` by `human:Angel Gonzalez (projectsaturnstudios)`). Further Chassis restores that change the documented surface should flip it back to `draft` until re-verified. [contracts](components/contracts.md) remains `draft`.
* **Creation**: Draft [chassis](components/chassis.md) — PSR-11/`ArrayAccess` container how-it-works (`get` vs `make`, resolve city, bindings/singletons/callbacks). Status `draft` until Angel verifies; notes missing `instance`/`alias`/`extend` and WIP `#[Cache]`.
* **Creation**: Draft [contracts](components/contracts.md) — `fabricate/contracts` inventory + Chassis split (`ServiceContainer` public; `WireframeServiceContainer` Chassis-local). Indexed in root + components indexes; [composer-replace](conventions/composer-replace.md) lists chassis/contracts/magic-aliases.
* **Update**: [MagicAlias and provider ownership](conventions/magic-aliases.md) — preferred model: domains stay independent; **Core** owns concrete MagicAliases + service providers (composition root). Prior suggest-in-domain sketch superseded for first-party packages.
* **Creation**: Stable [MagicAlias ownership](conventions/magic-aliases.md) — MagicAlias base = own component; concrete aliases live in respective domain packages. Verified by Angel.
* **Update**: Commented out Nab `Env` `.env` writers + Filesystem helpers; in-source note points at [dependency-direction](conventions/dependency-direction.md) “Env write ownership” for relocation to Broadcasting/Core.
* **Update**: [Component dependency direction](conventions/dependency-direction.md) — Nab may use **moons only** (not Filesystem/other components). `.env` writes + Filesystem owned by future Broadcasting (websockets) / Core commands; Nab `Env` stays read-side. 0.6 has Broadcast hooks only (no component tree yet). Updated Nab concept + AGENTS.
* **Update**: Softened [Component dependency direction](conventions/dependency-direction.md) — Nab↔moons support clique; hard ban **nothing below Core depends on Core**.
* **Creation**: Stable convention [Component dependency direction](conventions/dependency-direction.md) (initial stricter form; see Update above).
* **Verification**: Angel marked [collections](components/collections.md), [conditionable](components/conditionable.md), [macroable](components/macroable.md), and [reflection](components/reflection.md) `status: stable` (`verified` by `human:Angel Gonzalez (projectsaturnstudios)`). [nuts-and-bolts](components/nuts-and-bolts.md) remains `draft`.
* **Rewrite**: Scrubbed moons-tier framing from all component concepts + package/composition/conventions. Each of [nuts-and-bolts](components/nuts-and-bolts.md), [collections](components/collections.md), [conditionable](components/conditionable.md), [macroable](components/macroable.md), [reflection](components/reflection.md) rewritten as a normal Fabricate component (how-it-works + identity). Shared `Fabricate\NutsAndBolts\` documented as packaging only.
* **Rename**: OKF section `moons/` → `components/`; `moon-packages` → `component-packages`; `moon-composer-drift` → `component-composer-drift`. Framing: these are Fabricate **components**; informal “NutsAndBolts moons” only meant siblings that currently share `Fabricate\NutsAndBolts\` — NutsAndBolts is one component among them.
* **Update**: Expanded [collections](components/collections.md) with a **How it works** section (eager vs lazy, entry points, EnumeratesValues pipeline, higher-order proxies, Arr, exceptions).
* **Update**: Refreshed [collections](components/collections.md) — component composer no longer requires `fabricate/contracts`; documented current macroable/nuts-and-bolts requires + umbrella helper autoload. Fixed leftover `Fabricate\Contracts\NutsAndBolts\*` imports in `LazyCollection`, `Arr`, and `EnumeratesValues` → `Fabricate\NutsAndBolts\Contracts\*`. Cleared `stale_after`.
* **Correction**: Angel rejected the hollow-snapshot OKF framing. Components, config, and component completeness are being restored this session; 0.6 kitchen-sink is a **donor**, not forbidden. Marked [component-composer-drift](traps/component-composer-drift.md) and [do-not-copy-0.6-okf](traps/do-not-copy-0.6-okf.md) deprecated; rewrote root [index](index.md) + [AGENTS.md](../AGENTS.md) for reconstitution. Full rewrite deferred until restore slice is chosen.
* **Initialization**: Created OKF v0.2 bundle for `scrapyard-io/framework` **0.7.0** (orientation, components, conventions, traps, playbooks). That pass assumed a permanent slim NutsAndBolts-only surface — **superseded** by the correction above.
* **Creation**: Package-root [AGENTS.md](../AGENTS.md) first updated for 0.7 OKF workflow, then corrected for reconstitution.
